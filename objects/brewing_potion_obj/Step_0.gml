/// @description Insert description here
// You can write your code in this editor
if ( canWin ) {
	if ( potionType && colorMatch ) {
		event_user(1);	
	}
	
	if ( global.moves >= 5 ) {
		event_user(2);	
	}
}