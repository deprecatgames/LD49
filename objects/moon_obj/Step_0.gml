/// @description Insert description here
// You can write your code in this editor
if ( place_meeting(x, y, times_up_obj ) ) {
	with ( time_controller_obj ) {
		canCount = false;
		event_user(1)	
	}
	instance_destroy();
}